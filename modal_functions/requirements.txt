# Pipeline de análise de ECG — ProECG
torch>=2.0,<3
torchvision>=0.17,<1
numpy>=1.24,<2
scipy>=1.10,<2
neurokit2>=0.2,<1
opencv-python-headless>=4.8,<5
requests>=2.28,<3
Pillow>=9.0,<11
modal>=0.60

# Open-ECG-Digitizer (vendor): SignalExtractor usa skimage.measure.label
scikit-image>=0.21,<1
# Open-ECG-Digitizer (vendor): LeadIdentifier carrega layouts de YAML
PyYAML>=6.0,<7

# OCR (correção de orientação)
easyocr>=1.7,<2

# Motor 2 — Claude Vision API
anthropic>=0.40.0

# Dev server (opcional, não precisa em produção)
fastapi>=0.100
uvicorn>=0.20
